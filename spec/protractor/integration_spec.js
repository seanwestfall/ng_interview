describe('protractor test', function() {
  it('search and select three movies', function() {
    // check to see if caching is working
    // search for batman
    // search for die hard
  });
  it('search then clear search bar', function() {
    // check to see that you can clear the search bar
  });
});
